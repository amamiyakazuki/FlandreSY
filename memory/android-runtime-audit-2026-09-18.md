# Android 运行逻辑排查记忆（2026-09-18）

- 用户要求本轮仅检查、记录 bug 和制定计划；后续按用户确认逐模块实施、验证、更新计划，不能把历史发布授权沿用到本轮。
- 用户补充热水现象为“有概率启动失败，但是不知道原因”，没有确认具体误报类型。不能把 BLE 竞态或历史订单语义风险直接当作已证实的现场根因。
- 已读取 Agency Agents 安装角色：Mobile App Builder（唯一指导中明确包含 Flutter 的已安装角色）、Code Reviewer、Evidence Collector；分别独立检查饮水、潜在缺陷和测试证据。没有专用 Flutter/Dart 角色。尚无界面重设计需求，未启用 UI Designer。
- 主要证据：BLE 写完成后才订阅非重放通知流，存在快速响应漏接窗口；缺少贯穿启动步骤的诊断；饮水离页停止 timer，冷启动/重进没有恢复；HTTP 请求缺少明确截止时间。
- 热水“订单出现即结束”是当前规格和测试的既有约定，服务端是否只在结算后返回记录尚未证实。调整需用户确认和契约证据；不能用当前测试通过证明该约定正确。
- 现有检查：分析无诊断，36 项测试通过。无 Android 设备；未做真实开关水、支付、接口请求、截图或真机验证。饮水 timer/重启/账号并发和真实 BLE 时序缺少覆盖。
- 当前正式问题记录是根目录 bug.md 的 2026-09-18 章节；P_PLAN/bug.md 为历史记录，不能把历史已处理标签当作边界问题已解决。
- 计划在 P_PLAN/PLAN.md 最新章节。该路径被 .gitignore:75 忽略，写入本地不等于会随 Git 提交；是否改为跟踪待用户选择。
- 建议先 M1-A 热水启动可靠性与诊断，再分模块修复。轮询生命周期、终态证据、重复下单、旧订单账号归属和设备匹配策略均未获批准。
