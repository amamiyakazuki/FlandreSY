# 热水手动控制与恢复规则（2026-09-19）

- 2026-09-20最新收口：用户明确热水现场验证通过，要求结束本轮并进入CI。按用户反馈记录，不冒称代理实际设备验收；支付明确不再验证，不改支付业务。历史待复测表述由本条更新。

## 2026-09-20 双击幂等修正（覆盖下方重复发送规则）

- 按钮可点不等于每次都重发协议。相同账号/设备active或uncertain会话再次开始只反馈，不重发、不换时间/isn；成功停止并清理后才可新启动。尚未发送的失败仍允许重试。
- 连续控制同类请求合并，恢复不破坏合并，stop/明确本地清理是屏障；统一start/stop入口等待，保留同tick开→关→开顺序。
- 用户连续双击后实体停水；测试已复现重复启动调用，但没有真实协议日志证明现场停水根因。40分钟恢复、缺凭据仅更新订单、按钮始终可点规则不变。
- 发送阶段保存失败先保留内存uncertain和凭据，防止再次启动；无法落盘会提示勿关闭App，不声称跨进程安全。

## 历史实施记录

- 用户在接续任务中选择 M1-B，并明确覆盖旧结束判定：任何时候都能点击开始热水和结束热水；关闭 App 再打开，距开水超过 40 分钟则恢复初始待开水状态并更新订单，40 分钟以内保持已开水状态。
- 旧“订单出现即结束”以及先前推荐的“只有明确终态才结束”不再作为本次实现目标。用户没有要求 40 分钟后自动向设备发送关水命令。
- 已有不做真机验证、业务修复后再讨论 CI 的限制继续有效。
- 用户最终明确：缺关水凭据不用管，更新订单即可；不把协议恢复研究设为实施前置条件。消费历史接口不依赖本地 isn；本地状态缺失不等于远端订单消失。
- 已实现：开关按钮始终可点；请求串行，连续相同点击合并；缺 session/isn 的住理停止只刷新订单，显示刷新结果；有凭据正常关水。查询不再自动清除活动会话；移除基线查询和热水定时轮询。
- 恢复边界：恰好 40 分钟保持已开水，超过才清本地状态并刷新原账号可用的住理历史。只在启动/回前台恢复时评估，不在持续前台时自动到点变更。798 没有已接入历史接口，不混查住理历史。
- 会话时间在尝试发送启动指令前记录；重复开水发送前失败保留原记录，新的开水发送后使用新时间。控制、恢复与本地清理串行，设置删除失败不先丢凭据。
- 验证完成：全量 59 项测试、分析无诊断、OpenSpec 严格校验与 diff 检查通过。未做真机、构建或 CI；原 HW-07 BLE 匹配策略留待后续。
- 已找回历史资料位置：`54274c1^:docs/service-interfaces.md`，以及同版本 `app/src/main/java/com/kazuki/zhulihotwater/{LegacyHotwaterActivity,HistoryActivity,HotwaterActionRunner}.java`。当前工作区文件已删，但 Git 保留。
- 资料只证明握手返回 isn、消费历史可查询、现有结束流程使用 isn；尚未证明历史包含活动订单或 isn、重新握手能关闭既有订单。不得将当前实现限制推断成服务端协议限制，也不得将被拒绝方案记作用户同意。
