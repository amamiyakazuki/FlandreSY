
# 2026-09-20 真机反馈：连续两次开始热水后卡住并结束（用户现场验收通过，已收口）

- 最新反馈：用户明确已验证通过并要求收口；支付本轮不验证，不代表发现支付故障或已完成支付验收。下方“待复测”为此前历史进展；实体停水的原始因果仍无协议日志证明。

- 用户补充连续点击两次，界面没看清，实体水流确实停止；此前按住理上下文处理，但该补充原文为“助理”，不当作系统名称的新确认。不能仅凭代码断言现场根因。
- 代码确认：_enqueue 仅合并在途且相邻同key请求，完成后清除key；resumeHotwaterSession无key入队也会改写_lastQueuedKey。_startSession没有“同账号同设备已经启动”的业务幂等保护，第二次会新建会话并重新走协议；现有测试明确允许完成后再次调用start adapter。
- 住理重复启动会重做握手、可选旧订单同步、创建订单及启动确认；旧订单同步包含end_consume_response，但没有日志不能据此认定它造成现场停水。
- 之前把“按钮随时可点击”实现成“完成后每次点击都重新执行启动协议”，未区分可点击交互与业务幂等，是本轮需纠正的设计缺口。此前124项测试通过不能证明重复启动真实设备安全。
- 已先复现开始→恢复→开始实际调用2次（期望1），随后修复在途合并和执行时幂等。commandSent后保存持续失败也先保留内存uncertain，不丢关水凭据；跨App退出的存储失败仍有风险，提示勿关闭App。
- 新回归又发现同tick开→关→开被合并：stopActiveHotwater多一次await ready导致入队晚于后点的start，已统一入口等待次数。798及缺凭据已有会话的两条旧测试期望重复start，已随新规则更新；不是硬件验证。
- Evidence Collector独立全量129项、热水专项40项通过；一处测试fixture花括号分析提示已修，主代理复跑分析无诊断及diff检查通过。首次brace补丁因格式化后的换行未命中，按实际内容重试成功；无业务改动丢失。
- 已重建共存debug arm64 APK并验证包名/签名。现场停水原因仍待日志/用户新包复测，不宣称已证明。

# 2026-09-19 Android debug/release 共存

- 原debug和release都使用com.flandresy，会覆盖安装或因签名不同安装失败；已给debug添加.debug包名后缀及独立显示名，release身份保持不变。
- debug arm64 APK构建成功，签名v2校验通过，最终manifest启动类与provider authority已核对；未做手机安装/真实支付测试。
- 构建警告：mobile_scanner/package_info_plus仍应用Kotlin Gradle插件，Flutter提示未来版本需迁移；当前构建通过，未改依赖。初次rg附带不存在目录返回2，已用实际路径核对，不影响构建。

# 2026-09-19 Agency 协作实现与审查

## 最终业务修复状态

- AUTH-01/02/03/04、WS-01/02、ORD-01/02、DATA-01、HW-07、NET-01 的本轮代码修复已完成；DW-01/02/03/04 与热水新规则回归保留。CI-01及真机现场验证没有伪标完成。
- Evidence Collector 最终全量124项通过，独立新增21项验收；Code Reviewer最终无新阻断项。此前104项和其它中间计数不再作为最终验收计数。
- 确定复现并修复的新增故障：798迟到设备响应清新token；洗衣创建后详情失败丢ID；并发只读401打断成功副作用记录。保留对应回归测试。
- 最后静态分析发现7处curly规范提示，已逐文件补括号；BLE精确上下文补丁与测试fixture工具错误均已闭环。
- 只读查询落盘期间短暂阻止切账号，避免旧存储写入迟到覆盖新账号数据；网络查询阶段仍允许切换并按epoch隔离。保全落盘失败清楚提示仅内存、勿关闭App。
- 数据损坏不会自动删除服务端/本地旧订单；读取不了订单时选择阻止覆盖并告警。这是保护性降级，不代表自动修复了未知损坏内容。
- 未做实际设备/真实接口/支付验证；未知第三方状态码仍保守保留，不猜终态。最终静态分析结果记录到PLAN验收章节。
- 最终收口：Evidence Collector 报告 `flutter analyze --no-pub` 无诊断，`git diff --check` 通过；全量124项通过。当前代码修复结果与现场尚未验证的边界分开记录。

## 实施过程记录

- 用户授权主代理自主推进业务修复、选择分歧并告知；未知旧活动单仍按 A 明确确认归属。UX Architect 已完成只读流程梳理；Mobile App Builder 负责订单，Code Reviewer 与 Evidence Collector 分阶段审核/验收。
- AUTH-02 补充外围路径：devices_actions 刷新旧401没有epoch保护；798 loadDevices 内部直接清token绕过runtime代次；798登录成功后加载设备失败会混合新token/旧账号。已分别修正并补独立复现测试。
- AUTH-03 补充：账号保存忽略prefs bool失败、账号/token分别写入中途退出混合身份、设备选择与登录保存交错。已检查写结果，保存新token前失效旧普通metadata、手机号最后提交，登录完成落盘后才emit成功；设备设置入口受认证锁/写入计数保护。
- ORD-02 / P1：洗衣创建返回ID后详情失败仍丢ID，与DW-02同类；审查发现，交 Mobile App Builder 修最小待查询快照。
- AUTH-04 / P1：订单副作用在途时，其他只读请求401可改变epoch，使成功创建ID被丢弃；已选择保全已发出操作的结果及原owner，不继续控制，保留需登录提示。正在补并发复现。
- DATA-01：bootstrap已改独立存储域恢复，订单损坏阻断覆盖；非订单坏JSON保留原件和恢复备份；secure读取按服务降级；全局恢复告警与脱敏日志已接入。洗衣旧非终态历史须成为待确认候选而非自动当完成历史，正在补迁移。
- HW-07：目标MAC优先，缺MAC只允许指定名称精确匹配，删除任意XN回退；初始化失败断开，发现服务/通知启用增加期限；UUID精确标准化。审查补发现早CRC错误可未处理，已提取BLE响应helper同时观察写入/响应及通知流错误，独立测试通过。
- NET-01：U净/798新增15秒总期限覆盖连接、响应头/正文，迟到连接中止，验证码也覆盖；不自动重发控制/下单。独立fake HttpClient测试无真实外部网络。
- 验证中间状态：Mobile 104项全量通过后还有审查修订，不能作为最终结果；Evidence新增7项业务验收、8项IO/BLE验收通过，最终全量待稳定后重跑。
- 工具/测试过程：dart fix一次传多个路径被拒，改逐文件后成功；Mobile初次引用不存在测试路径及Future类型推导错误已修；Evidence首次fixture类型编译错误已修。以上均不代表真实设备故障。
- 审查闭环：ORD-02/AUTH-04 已修并通过专项；保全结果落盘失败额外显示仅内存/勿关闭提示。只读查询提交存储期间也短暂阻止切账号，避免旧保存迟到覆盖新快照。旧洗衣非终态历史按顺序恢复待确认候选。
- 工具补充：BLE补丁两次因格式化/注释上下文不一致未应用，重新读取后使用精确上下文完成；无失败补丁残留。最终不以中间测试计数作为交付依据。

# 2026-09-19 M3 账号核查（历史核查阶段）

- AUTH-01：M2-B 已退出饮水认证失败 loading；洗衣 scan/create/pay/start/stop/cancel/refresh 仍直接失效后返回，需要统一处理。不能把局部修复写成账号模块已完成。
- AUTH-02：已复核 U净无认证代次，旧 authInvalid 会清新 token；饮水与洗衣持久化订单缺 owner。迁移策略为真实业务分歧，待用户在“确认归属后恢复”与“保留为只读旧记录”间选择，不自动归给当前登录。
- AUTH-03：AccountSessionRepository 没有失效标记或清理接口；handleAuthInvalidation 只清敏感凭据与内存，_applySnapshot 仅凭账号资料展示成功。修复需同时覆盖三套服务及预加载/异步启动路径。
- 此次仅核查和规划，未修改业务代码、未调用真实登录/订单接口，未运行新增测试。

# 2026-09-19 M2-B 饮水订单核查与修复

## 最新结果（覆盖下方核查时的待实施说明）

- DW-02 已修复：创建返回最小订单，先保存再查详情；详情失败仍保留 ID，测试确认重启继续查同一订单、不重新下单。
- DW-03 已修复：仅状态 50 为确认终态；未知状态、“未完成”“等待结束”等文案不清单。
- DW-04 已修复：手动/自动/扫码前刷新共用同一在途查询，避免并发响应回写。离页/后台/回前台轮询回归通过。
- ORD-01 已按用户 A 修复：旧订单查询失败或未完成时禁止新建；完成且历史保存成功后才下新单，重复扫码不重复创建。
- 实施中补充：原持久化 fire-and-forget 无法保障先保存后查详情，现等待完成；完成历史保存失败不清当前订单，初始保存失败仍保留内存 ID 并提示暂勿关闭 App。保存恢复后刷新可恢复轮询。这不等同于已经解决所有存储损坏与操作系统强杀风险。
- 饮水认证失效分支现在会退出 loading；M3 跨账号响应失效和订单归属问题仍未实施。共享网络总超时 NET-01 未在本模块扩展。
- 验证：新增饮水专项 13 项，全量 72 项测试通过；没有真实设备/接口验证。此次未出现失败测试。

- DW-02：真实适配器取得创建接口 orderId 后还要等待详情接口，详情异常会使整个创建动作失败，运行时没有保存已创建订单。计划拆开创建保存与详情查询；服务端订单存在不等于本地还能找到它。
- DW-03：WaterOrderUi.isTerminal 使用 contains('完成') / contains('结束')，会误中“未完成”“等待结束”；历史抓包只确认 50 为终态，计划删除文字猜测。
- DW-04：自动刷新才设置 _waterPollInFlight，手动刷新不受该标记保护；自动请求在途时手动刷新可并发，存在迟到状态回退风险。计划统一并发保护。
- ORD-01：扫码只检查 waterOrder.isBusy，不检查已有 currentWaterOrder，新的创建结果可覆盖尚未结束的旧订单。单活动订单与多活动订单属于产品分歧，暂停待用户选择，未擅自修改行为。
- 证据：已读取 Git `54274c1^:docs/ujing-water-flow.md`；有按 ID 查询详情接口，消费列表接口契约未在该文档中验证。上述为代码核查，不是真实设备测试结果。

# 2026-09-19 M1-B 新规则核查

## 本次处理结果（最新状态，覆盖下方核查时的待定说明）

- HW-03：已按用户新规则删除订单结束推断、基线门禁与热水自动轮询；恢复显示使用严格大于 40 分钟的时间窗口。
- HW-04：错误清理只清所属提示，停止失败后仍显示已开水；对应时钟测试通过。
- HW-05：控制、恢复与本地清理使用同一串行队列，清理执行前复核会话；处理中隐藏本地清理入口，开关水按钮仍可点。
- HW-06：先删除持久化会话，再清理 isn；设置删除失败会保留凭据，故障注入测试通过。凭据删除失败记录诊断，不把它伪报成设备关水失败。
- HW-08：用户最终要求缺本地凭据时只更新订单，已实现并验证无 session 与有 session/无 isn 两种情况；无需继续等待凭据恢复协议。
- HW-09：首页/详情页在 idle/running/loading 均可点；连续相同请求合并、相反请求顺序执行；正常凭据关水保持原协议。
- 测试过程记录：移除旧 `hasNewConsumption` 后，首次分析发现旧测试仍引用该方法，已替换为“查询不改变开水状态”断言；widget 测试首次因在 runAsync 中等待 fake 时钟微任务而挂起，终止后改用 pump 推进，重新通过。一次补丁因首页上下文不匹配未应用，核对后修正，无半成品遗漏。
- 最终验证：Flutter 分析无诊断，全量 59 项测试通过（热水专项 35 项），OpenSpec 严格校验和 diff 检查通过；未做真机验证。HW-07 设备匹配及其它模块问题仍保留。

- 2026-09-19 纠正：用户拒绝缺少本地凭据时直接提示无法关水的方案。已在 Git `54274c1^` 找到被删除的 `docs/service-interfaces.md` 与旧 Java 源码，确认消费记录查询已存在且不依赖本地 isn。本地清理没有远端删除调用，不能声称本地会话丢失会使服务端订单消失。当前资料尚不足以证明历史记录覆盖活动订单或支持恢复 isn；需区分“当前实现不支持”与“服务端不支持”。
- DOC-02 / 资料缺口：当前 Flutter 注释引用的 service-interfaces.md 已于 `54274c1` 被删除；历史源文档可从 Git 读取。后续接口核查应检查 Git 历史，不能只凭工作区不存在就判断资料缺失。

- 用户已选择取消旧热水结束判定，允许随时点击开始/结束，重开 App 时按距开水是否超过 40 分钟恢复待开水或已开水，并在超过时更新订单；旧 HW-03 的候选方案不再代表当前需求。
- HW-08 / 实现约束：`RealZhuliAdapter.stopHotwater` 必须拿到开水握手生成的 isn。只解除 UI 禁用不能保证无凭据时可以关水；现有 `clearHotwaterSessionLocally` 还会删除 isn，不能直接复用它完成 40 分钟显示重置，否则随后点击结束将失去控制凭据。凭据保留/缺失反馈方案待确认。
- HW-09 / 交互缺口：首页、详情页按 busy/running/session 禁用按钮；actions 又在 `_sessionOperation` 时直接返回。只修改按钮会导致“可点击但请求被静默忽略”；操作中点击采用合并/顺序执行，还是明确提示稍后重试，待用户选择。
- 本次只读检查与规划已完成，业务修复和新规则测试尚未执行。
- 工具使用记录：上轮读取 Codex 任务时 turnLimit=20 超出接口上限 10，已改用允许值读取；后续按需分页或筛选消息以避免输出截断。

# 2026-09-18 Android 运行逻辑排查（仅诊断，待确认修复）

用户补充：热水是“有概率启动失败，但是不知道原因”，暂不确定属于假失败、真实失败或状态误报。因此不能将以下任何一项直接宣称为现场唯一根因。

证据等级：**代码确认**表示控制流或数据判断可直接证明；**条件风险**表示缺陷触发需要特定响应/时序，尚无现场证据。所有新增项均未修复。P1 优先处理，P2 后续处理；暂未发现足以确认的 P0。完整实施范围与待选方案见 `P_PLAN/PLAN.md` 最新章节。

## CI-01 / P2：现有 CI 未覆盖 Flutter/Android 质量门禁

- 用户已选A，新增`.github/workflows/flutter-android-ci.yml`：只读权限、固定工具与Action SHA，依次执行锁文件解析/分析/测试/Debug ARM64构建，只上传APK7天。旧iOS与发布逻辑未改，未配置强制分支保护。
- 审查无阻断项；setup-java v4官方已提示弃用，作为后续维护项记录，不在本轮扩大到现有工作流升级。现有版本已固定SHA；远端执行仍需首跑验证。
- Evidence Collector独立lint/12项配置断言/APK签名与ARM64核验通过。其GOPROXY=off尝试因Go模块弃用检查失败，恢复标准go run后通过，不是workflow故障。
- 本地验证：pub --enforce-lockfile成功且锁文件未变；分析无诊断；129项测试通过；同CI Gradle参数的Debug构建通过；actionlint v1.7.7及YAML解析通过。构建仍有此前KGP未来兼容警告，未升级依赖。
- 边界：macOS本地不是Ubuntu托管runner；未提交/推送/运行GitHub远端，CI实际成功与artifact下载需首轮远端执行验证。临时Debug签名不保证覆盖已有安装，不自动传入本机密钥。

- 2026-09-20进入本阶段：现有唯一workflow为iOS build，缺analyze/test及Android门禁。已写P_PLAN方案，等待用户选择debug门禁或额外release编译检查；尚未实施workflow或远端运行。
- 现状：仓库存在 `.github/workflows/ios-build.yml`，但本轮检查未发现覆盖 Flutter 分析、全量测试、Android 构建或 APK 校验的对应工作流；本轮这些检查均由本地执行。
- 影响：Android 相关提交没有自动重复执行静态分析、测试、构建和产物校验的统一门禁，结果依赖个人本地环境。
- 边界：这不是热水或饮水运行时根因，也不在本轮修复范围内；CI 平台、Android 验证深度和失败通知方式尚未决定。
- 计划：M1-A、M2-A 及其验证完成后，单独盘点脚本和工作流，再由用户选择 GitHub Actions、本地脚本优先级，以及分析/单测、Android 构建、模拟器/真机验证的分层门禁。

## HW-01 / P1：BLE 写入后才监听响应，存在快速响应丢失窗口
- 证据：`lib/src/data/adapters/real_zhuli_adapter.dart:154`、`:226`、`:270` 都先 await 写入再 await 通知；`lib/src/data/adapters/flutter_blue_plus_ble_transport.dart:184` 才订阅 `notify.onValueReceived`。本机 flutter_blue_plus 源码确认该流不会重放订阅前事件。
- 触发：握手/启动/停止响应先于 write Future 完成到达，后续监听收不到已经发出的响应，6 秒后报超时。设备可能已执行，但 App 认为失败或待确认。
- 等级：代码确认存在时序窗口；是否为用户这次偶发失败根因待 Android/BLE 证据。
- 建议/验收：在写入前建立本次请求的响应等待，隔离过期帧、超时和断连；用可控通知流覆盖早响应、正常响应、迟到响应和取消，随后真机复测。不能简单重放最后一帧或自动重发启动命令。
- 2026-09-18 处理进展：已在 `ZhuliBleConnection` 增加写入前等待接口，真实 flutter_blue_plus 连接先订阅通知再写入；握手、费率、历史、启动和停止路径已切换。自动测试通过，真机快速响应仍待验证。

## HW-02 / P1：启动诊断不完整，关键失败阶段被泛化提示掩盖
- 证据：`hotwater_actions.dart:182` 的 catch 将非 HotwaterException 变为“启动热水失败”；已到 dispatch 边界时统一显示“启动指令已发送但结果待确认”。`real_zhuli_adapter.dart:396` 仅有 HTTP endpoint 失败日志，BLE 传输与 action 缺少阶段、耗时和异常类型的关联记录。
- 影响：无法区分历史基线、设备查询、扫描、连接、握手、旧订单同步、创建订单、写指令及确认失败；用户只能看到偶发失败，无法定位。
- 等级：代码确认。证据不足应保留待确认，但不应丢掉诊断原因。
- 建议/验收：为一次启动关联分阶段、脱敏的诊断记录，页面给出具体失败步骤和可理解的处理方式；保留“指令是否可能已发出”的区别。记录不包含密码、token、isn、原始控制报文；逐阶段故障注入校验文案与记录。
- 2026-09-18 处理进展：已为住理启动流程增加设备查询、扫描连接、握手、费率、历史、订单创建、启动确认和完成阶段日志；接口错误继续记录 endpoint 与错误类型。页面文案和 Android 真机验证仍待后续。

## HW-03 / P1 条件风险：把订单出现直接视为热水结束
- 证据：`lib/src/runtime/hotwater_state.dart:104` 已知 orderId 仅做 ID 相等判断，不检查设备、时间及 status；无 orderId 时仅检查同设备、新 ID 和时间。`hotwater_actions.dart:301`、`:312` 据此清会话及关水凭据。`test/hotwater_session_test.dart:165` 甚至把“进行中”新订单和同 ID 的未知状态记录断言为结束证据。
- 触发条件：历史接口在结算前返回创建中/运行中的订单，即可误清真实活动会话。若接口保证只返回已结算记录，则需要先用契约证明该保证，不能仅凭方法名假定。
- 影响：App 失去当前会话及关水凭据，可能误显示已结束。
- 决策点：按已核实终态结束；或资料不足时保留待确认、只通过明确关水成功/用户确认清理。现有 OpenSpec 和测试采用“订单出现即结束”，修改前须用户确认并同步规格，不能只改代码。

## HW-04 / P1：停止失败后的提示清理误写“热水待启动”
- 证据：`hotwater_actions.dart:423` 的定时回调只要 start 或 stop 仍为错误，就同时把 start 改为 idle/“热水待启动”，不检查现有 session/running。
- 触发：活动热水关水失败，3 秒清提示先于下一次 10 秒轮询；仍有活动会话，却显示待启动。旧 stop 错误还可能覆盖稍后开始的新操作状态。
- 等级：代码确认。
- 建议/验收：清理提示只处理其所属动作与请求，状态标题从会话事实派生；测试停止失败、3 秒内重试、继续供水以及轮询恢复。未知状态不能改成已停止。

## HW-05 / P1：热水启动期间仍可清除会话和关水凭据
- 证据：`lib/src/hotwater/hotwater_detail_screen.dart:38`、`:75` 仅按 phase 显示清除按钮，未禁用 busy；`hotwater_actions.dart:231` 清除入口没有 `_sessionOperation` 守卫。
- 触发：已持久化 uncertain、正在等待 BLE/HTTP 确认时，用户确认本地清除；在途启动无法因此撤回，后续回写被 session 校验丢弃，设备可能仍启动而本地没有会话。
- 等级：代码确认竞态，需可控异步用例复现。
- 建议/验收：清理与启动/停止互斥，确认后再次检查当前会话与操作状态；验证清除对话框期间阶段变化、在途写入和返回结果。

## HW-06 / P1 条件风险：删除凭据成功而删除会话失败后，恢复入口不一致
- 证据：`lib/src/runtime/shui_runtime_base.dart:140` 先删 isn 再删设置会话；后者异常保留原 phase。active 会话的本地清除入口被 `hotwater_detail_screen.dart:39` 隐藏；运行时清除方法却允许缺 isn 的 active 会话。
- 影响：存储部分失败后会留下无关水凭据的 active 会话，当次前台无法通过详情入口解除；重启恢复逻辑可能把它降为 uncertain，但不能依赖重启补救。
- 建议/验收：明确定义部分成功的恢复状态与补偿处理，让界面和运行时共用清理资格；分别注入 secure/settings 失败验证不虚报已关闭。

## HW-07 / P2：BLE 目标匹配与连接失败清理存在隐患
- 证据：`flutter_blue_plus_ble_transport.dart:106` 名称匹配、MAC 匹配、XN 名称兜底为 OR；即使有明确 MAC，也可能先选到同名或兜底设备。`:38` 连接成功后查服务/特征或开启通知失败，没有 finally 断开；adapter 只有拿到 connection 后才进入 finally。
- 等级：代码确认匹配/清理缺口；邻近同名设备时是否发生误连待现场验证，不能据此声称错误设备已经出水。
- 建议/验收：明确有 MAC 时的优先级与无唯一身份时的策略，初始化失败确保断连；覆盖多个候选设备、缺服务、缺特征、通知开启失败。若证实误连会执行控制，优先级升为 P1。

## DW-01 / P1：离页、重进和重启丢失饮水自动轮询
- 证据：`water_actions.dart:218` 离页清理取消轮询；`lib/src/shell/shui_shell.dart:185` 返回时调用，`:498` 重进仅导航。`shui_runtime_base.dart:79` 恢复仅启动热水，`:329` 恢复饮水订单但无饮水 timer；`:450` 前台恢复同样只补查热水。
- 触发：创建未完成订单后离页/重进，或保存订单后杀进程重启；订单仍显示，但等待超过 5 秒不再自动查状态。仍存在的 timer 可以在前台继续 tick，不应笼统声称所有前后台切换都会停。
- 等级：代码确认，直接解释“不轮询”的常见路径。
- 建议/验收：轮询跟随活动订单、由 runtime 管理；冷启动/前台/同账号重登立即补查再恢复 5 秒周期。另一方案为仅页面内轮询但所有重进入口恢复，待用户选择。测试离页、重进、两种启动恢复路径、前后台及终态停查。
- 2026-09-18 处理进展：已将轮询生命周期上移到 runtime；离页清理不再停止活动订单 timer，冷启动、前台恢复和重进饮水页会立即补查并恢复 5 秒周期；无活动/终态订单停止 timer，自动轮询增加 in-flight 互斥与订单代次校验。flutter analyze --no-pub 通过，flutter test --no-pub 36 项通过，git diff --check 通过。仍缺 Android 真机/模拟器、真实 U 净接口和进程被系统终止后的现场证据。

## 2026-09-18 M2-A 实施边界
- 本模块只修复饮水轮询生命周期，不处理创建成功但详情失败的订单交接（DW-02）、终态文案误判（DW-03）或账号代次隔离（AUTH-02）。
- 本轮已有自动化回归只证明现有运行时契约未回归；尚未补充专门的 fake-timer 生命周期断言，不能据此声称已自动复现离页/重启/前后台轮询。仍不能证明 Android 后台限制、网络超时和真实服务端状态码语义。

## NET-01 / P1：请求无明确截止时间，可能长期锁住轮询/启动
- 证据：`lib/src/data/adapters/io_ujing_transport.dart:45`、`:83` 连接、响应头、正文没有应用截止时间；`water_actions.dart:120` 互斥锁只在 await 返回后释放。住理/798 的 `io_*_transport.dart` 同样没有显式请求截止时间。
- 触发：服务端建立连接但不返回响应头或不结束正文；饮水后续 tick 被 `_waterPollInFlight` 跳过，热水可能停在历史查询或启动阶段。
- 等级：代码确认缺少期限；用户现场是否遇到挂起未知，系统底层可能自行超时，但不能当作应用可控保障。
- 建议/验收：限定请求总时长并取消底层请求，映射可恢复错误，解锁并保留订单/会话；查询允许后续重试，创建和控制操作不得盲目重发。使用本地受控响应验证断网、响应头挂起、正文挂起与迟到结果。
- 2026-09-18 处理进展：`IoZhuliTransport` 已增加可注入的 15 秒总超时，覆盖连接、响应头和正文读取；超时中止当前请求并返回错误，不自动重发可能已经到达服务端的控制请求。U净和 798 传输仍属于后续模块范围。

## DW-02 / P1：已创建饮水订单在首次详情失败后丢失
- 证据：`lib/src/data/adapters/ujing_http_adapter.dart:174` 成功创建拿到 ID 后，`:186` 必须详情成功才返回；`water_actions.dart:40` 只捕获为失败，直到 `:86` 才保存订单、`:98` 才启动轮询。
- 触发：创建成功，紧随详情请求失败；App 无订单 ID、无轮询，提示却像整个创建失败。
- 等级：代码确认。再次创建是否重复扣款/被服务器拒绝未验证。
- 建议/验收：创建成功先交接并持久化待确认订单，再单独刷新详情；详情失败只重查现有 ID。测试创建成功→详情失败→重启恢复，不再次提交创建。

## DW-03 / P2 条件风险：文案包含“完成/结束”被当作终态
- 证据：`lib/src/runtime/models/water_order.dart:70` 使用 contains；`water_actions.dart:172` 随后入历史、清订单并停查。非终态码配“未完成”或“等待结束”是确定的逻辑反例。
- 限制：未证明真实接口使用这些文案，不列作本次现场已确认根因；关联历史 `P_PLAN/bug.md` BUG-007，仍未根治。
- 建议/验收：根据核实的状态码表判终态，未知保留；覆盖正常完成、否定文案、空值、未知码。完整终态码需接口证据。

## DW-04 / P2：手动刷新与自动轮询并发可倒退状态
- 证据：`water_actions.dart:120` 仅自动轮询检查互斥锁；`:167` 仅验证订单 ID/代次，同订单两次请求可逆序覆盖非终态。
- 建议/验收：手动/自动共用请求互斥或响应版本；用两次可控异步结果逆序返回验证。已有终态清空订单 ID 能拦截后续旧响应，不声称“已完成订单必然复活”。

## ORD-01 / P1 条件风险：活动订单可被新建订单覆盖
- 饮水证据：`water_actions.dart:19` 只挡 busy，已有 currentWaterOrder 仍能创建；`:86` 直接替换；`shui_shell.dart:520` 快捷入口会创建。
- 洗衣证据：`washer_actions.dart:75` 创建只检查 washerOrder，不检查 washerPayment；`lib/src/washer/washer_order_screen.dart:193` 同样如此。支付 A 挂起期间创建 B，A 支付返回被 `:185` 的订单守卫丢弃，失去 A 的支付跟进和自动启动。
- 等级：客户端缺少活动订单保护已确认；服务器是否允许并行创建待验证，不能宣称已发生重复扣费。
- 决策/验收：建议单活动订单保护、同设备进入原订单，或用户明确选择多订单管理（范围更大）。覆盖重复扫码、不同设备、支付中重建和迟到响应。

## AUTH-01 / P1：鉴权失效遗留 loading，重新登录不能恢复业务
- 证据：`water_actions.dart:31`、`:135` 和 `washer_actions.dart:95` 等先置 loading；authInvalid 分支直接调用 `shui_runtime_base.dart:229` 返回，该处理只停 timer/清账号，不复位业务动作；`account_actions.dart:220` 重登也不复位。
- 触发：下单或手动刷新发生 401 → 重登 → `isBusy` 一直拦截；离页能重置部分状态，但不能可靠恢复轮询。
- 建议/验收：转换为 loginRequired 并保留活动订单，同账号重登恢复只读核对；覆盖饮水创建/手动/自动查询、洗衣创建/刷新/停止，不重放有副作用动作。

## AUTH-02 / P1：U净旧请求可清除新凭据，订单缺少账号归属隔离
- 证据：`account_actions.dart:182` 登录无 auth epoch；`shui_runtime_base.dart:230` 明确只为非 U净递增 epoch，`:235` 无条件清 U净 token；`water_actions.dart:131` 代次不随登录变化；饮水持久化模型未保存订单所属账号。
- 触发：A 请求挂起→B 登录成功→A 返回 authInvalid，新登录被清；或 B 凭据用于查询 A 留下的订单。
- 建议/验收：账号代次+订单归属双重校验，迟到成功/失效都不得修改新账号；迁移无归属旧订单需明确策略，不自动归给当前账号。使用可控异步与持久化测试。

## AUTH-03 / P2：失效后重启仍显示已登录
- 证据：`shui_runtime_base.dart:229` 清 secure 和内存，但未清普通账号 repository；`:286`、`:293`、`:300` 仅按快照账号存在恢复登录成功。三套服务均受影响。
- 建议/验收：账号资料与凭据有效性分开表达，或同步失效持久化状态；测试登录→失效→重启，不能显示有效登录。

## WS-01 / P1：离开洗衣页丢活动订单，其他请求也缺少统一回写保护
- 证据：`shui_shell.dart:185` 离页调用 `washer_actions.dart:441`，只保留 history，删除 currentOrder；`:202` 延迟启动因此可跳过。启动恢复 `shui_runtime_base.dart:331` 只恢复历史。创建/启动/停止/刷新回写见 `washer_actions.dart:130`、`:254`、`:303`、`:408`，未统一校验请求归属。
- 影响：已付款待启动/运行订单离页后失去控制入口；离页或新建期间旧响应可能重新写入过时状态，旧停止结果可能清理新订单。
- 建议/验收：明确活动订单持久化及归属，页面仅清瞬态；所有回写校验订单和账号代次。覆盖离页/重启、支付返回、自动启动、旧停止晚于新订单等情形。

## WS-02 / P2：刷新洗衣非终态 10/20/21 时保留旧状态
- 证据：`washer_actions.dart:423` 只在 status=40 时保留 refreshed，否则传 null；`lib/src/runtime/washer_state.dart:58` 把 null 视为沿用旧值。
- 触发：本地待支付 10，服务端已支付 20，刷新后仍显示 10；支付待确认不能正确转到启动操作。
- 建议/验收：所有非终态均更新订单，仅运行态需要更新时间戳；覆盖 10→20→21→40→终态。

## DATA-01 / P2：部分损坏快照或持久化错误缺少恢复处理
- 证据：`lib/src/data/water_order_repository.dart:54`、`shared_prefs_account_session_repository.dart:78`、`flutter_secure_session_repository.dart:53` 直接解码；`lib/main.dart:50`、`:90` 在 runApp 前等待，没有统一错误边界，单项损坏可能阻止启动。
- 另：`shui_runtime_base.dart:473` 的饮水保存和 `hotwater_actions.dart:393` 历史保存采用 unawaited，缺少明确失败处理；不据此断言平台写入一定乱序。
- 建议/验收：按存储项降级并报告恢复状态，重要订单保存结果可观察；不能用清空所有存储掩盖故障。覆盖非法 JSON、secure 读取失败、写失败、活动订单恢复。

## QA-01 / P1：关键状态机与轮询回归证据不足
- 已执行：`flutter analyze --no-pub` 退出码 0；`flutter test --no-pub` 退出码 0，36 项通过。日志：`/tmp/flandresy-audit-20260918/flutter-analyze.log`、`flutter-test.log`。
- 缺口：饮水仅有 `test/motion_transition_test.dart:205` 手动刷新测试，无实际 timer、重启恢复、异常重试、前后台测试；热水无真实通知时序验证，部分订单匹配测试固化 HW-03；账号仅覆盖热水切换后轮询，不覆盖请求中切换。
- `adb devices -l` 为空；`flutter devices` 仅 macOS/Chrome。无 Android 真机/模拟器证据，无本轮截图和 BLE/支付/真实接口验证；未发现 integration_test。通过现有测试不能排除用户反馈。
- 处理要求：测试随各修复模块补齐，不在本轮改测试；对每项标注代码证据、自动复现和真机结果，避免用静态分析代替业务正确性验证。

## DOC-01 / P2：诊断资料和跟踪记录存在断层
- adapter 引用的 `docs/service-interfaces.md`、`docs/ujing-water-flow.md` 当前项目不存在，无法据其核实终态码和订单生成时序。
- 根目录与 `P_PLAN/bug.md` 并存，历史“已处理”记录未覆盖本次发现的边界；本轮新增问题以根目录本节为准，保留历史上下文。
- `git check-ignore -v P_PLAN/PLAN.md` 确认 `.gitignore:75` 忽略整个 P_PLAN；计划文件已写入本地，但默认 Git diff/提交不会包含它。是否纳入版本管理待用户决定，本轮不修改忽略规则。
- 工具发现过程遇到不存在的 `~/.agents` 和 Agency Agents 的猜测子目录；已改从实际安装的 `~/.codex/agents/*.toml` 读取，角色可用性已核实。这不是应用故障。

---

以下为历史记录，历史“待处理/已处理”不替代上方本轮结论。

## 归档文档核对（2026-09-09）
- 同步后的用途说明需满足 OpenSpec 严格校验的至少 50 字符约束，已补充范围与行为说明。
- 热水 delta spec 仍引用旧实时接口优先方案，现按用户已确认的持久化会话与订单轮询决策修正。
- 主规格目录为空，账号入口与首页状态的 MODIFIED 声明没有对应基线，改为首次 ADDED 同步。
- 真机权限验证由用户确认完成，未声称本轮由代理执行。

## 账号中心圆形入口遗漏
- 已确认：AccountHubScreen 仍使用旧 AccountCard，路由已接通但未实现用户要求的分散圆形 App 入口。
- 修复范围：仅账号中心入口布局，保留已有账号详情和默认系统行为。
- 已修复并通过入口路由、圆形结构及窄屏大字体测试；此前只检查文字的测试未能发现视觉需求遗漏，现已补齐。
- 验证中修正：测试误 await 同步的 view.reset；798 登录页自动加载验证码有 400ms 模拟延迟，路由测试需推进时钟等待完成。

## 本次变更：account-permissions-and-status-cleanup
- 已接入统一真实权限服务，Shell 请求相机、蓝牙、通知权限，并在恢复前台时刷新；永久拒绝可打开系统设置。
- 默认洗浴系统新增 `none`，账号中心选择不再提前切换；住理/798 登录成功自动设为默认，账号页可取消，U净不改变偏好。
- 删除首页泛化警告、账号中心引导提示及 `GAL REVIEW REQUIRED` 历史标记。
- 热水订单/消费记录能否作为实时状态兜底仍需接口资料确认，暂未实现未知语义之外的猜测逻辑。

## 热水会话方案
- 已处理：热水进行中会话持久化，重启后立即核对并恢复轮询；订单变化作为结束依据。
- 限制：订单接口延迟或记录缺少可解析时间时无法确认结束，界面保留待确认；真机仍需验证服务端记录生成时序。
- 限制：本地终止进程期间无法后台执行 Dart 轮询，只能在下次启动/恢复前台时立即补查。

## 本次变更：refactor-app-motion-and-transitions
- APK 构建（2026-09-09）：全架构与单 ARM64 release 构建曾在 Dart AOT 阶段退出 -9；内存压力较高，使用临时 GRADLE_OPTS 限制 Gradle 堆为 1536 MB、workers=1、关闭并行后，ARM64 release 构建成功。未修改项目构建配置；当前 release 仍使用 debug 签名，版本仍为 2.0.0-beta+1。
- 已处理：并发启动 Flutter 命令曾触发 startup lock，iOS ephemeral `.packages` 清理提示；改为串行执行后 `flutter analyze`、`flutter test` 均通过。
- 已处理：单路由动画无法区分前进与返回；Shell 现在使用 Navigator/Page 详情栈及方向元数据。
- 已处理：支付 SDK 返回成功码但服务端订单仍待支付时会误显示成功；现在必须通过订单状态核验。
- 已处理：接水终态清理活动订单后结果丢失；新增按订单 ID 绑定的 `waterResult`，并隔离旧请求晚到响应。
- 限制：当前环境仅检测到 macOS 与 Chrome，没有 Android/iOS 真机，因此未执行 profile 帧时间、相机、蓝牙和真实支付宝回返验证。
## 2026-09-09 版本检查测试 Mock 响应编码
- 现象：测试中的 `MockClient` 直接以字符串字节构造响应，中文 JSON 在 `http` 解码时触发非法字符错误，误判为网络失败。
- 原因：`StreamedResponse` 的 body 必须是 UTF-8 字节流，不能直接传 `String.codeUnits`。
- 处理：改用 `http.Response.bytes(utf8.encode(...), 200)`，并补充远程清单成功、失败和 semver 回归测试。

## 2026-09-09 develop 合并后 iOS 构建失败
- 现象：`flutter analyze` 报 10 个编译错误，GitHub iOS workflow 无法进入有效的 Xcode 构建阶段。
- 原因：合并远程 2.0 整理与 2.1 重构时，启动快照、运行时依赖、热水详情路由和首页回调保留了不同版本的接口。
- 处理：统一饮水仓库、诊断日志和版本注入，补回热水详情路由与首页回调，并保留热水/饮水轮询和持久化；本地 `flutter build ios --no-codesign --release` 已成功。

## 2026-09-10 启动热水返回 `error_args`
- 现象：账号与设备码正常，点击“启动热水”后立即显示 `error_args`。
- 原因：启动动作在真实控制前新增历史订单基线查询，但 `consume/list_record_by_staffid` 的 `start`、`end` 被传为空字符串；旧版协议要求完整的最近 30 天时间范围。
- 影响：失败发生在历史基线查询阶段，尚未进入设备查询、BLE 握手或创建订单，因此容易被误判为账号或设备码错误。
- 修复：历史查询改传本地时间的最近 30 天范围，格式固定为 `yyyy-MM-dd HH:mm:ss`；增加 endpoint 级安全诊断日志与回归测试。
- 验证：请求捕获测试确认 `start`/`end` 非空且相差 30 天；全量测试和静态分析通过，release APK 构建及 v2 签名校验通过。真实服务端结果仍需安装后由用户验证。

## 2026-09-10 历史订单字段与来源失真
- 住理：真实字段为 `consume_money`、`create_at`、`status`，Flutter 误读为 `money`、`create_time`、`status_text`，导致金额统一回退 `¥0`、时间为空、状态伪装成“已结束”，错误值还会被持久化。
- 798：没有历史查询实现，但进入洗浴详情仍触发住理历史加载并展示共享缓存，可能把住理订单误认为 798 记录。
- U净接水：字段与旧版一致，但只保存本 App 创建并轮询完成的订单，不是账号完整历史。
- U净洗衣：字段与旧版一致，但历史仅存在当前内存；离开下单页或重启会丢失，且没有服务端历史列表实现。
- 处理：修正住理字段；隔离 798；准确标注 U净本机记录；补齐洗衣历史持久化。不猜测未知历史接口。
- 验证：住理真实字段与缺值测试、798 隔离 widget 测试、洗衣 codec/恢复/写入测试均通过；全量 25 项测试通过。

## 2026-09-10 洗衣历史测试存在未使用 import
- 现象：首次 `flutter analyze` 报告 `washer_history_repository_test.dart` 存在一个未使用 import。
- 原因：测试调整为直接使用 `PersistedSnapshot` 后，不再需要单独导入账号模型。
- 处理：删除多余 import，重新运行静态分析。
- 结果：`flutter analyze` 无问题。

## 2026-09-10 距离外启动热水形成永久异常会话
- 现象：设备不在蓝牙范围内时点击启动，页面长期显示订单查询失败或状态待确认；重启仍恢复该状态，停止又因缺少 `isn` 无法完成。
- 根因：代码在 BLE 扫描前即持久化 session；扫描、连接、握手、订单和启动确认的所有异常都被统一视为“可能已经启动”，没有保存启动阶段。重启逻辑又会无条件恢复 session 并启动轮询。
- 关联问题：`_pending` 无条件设置 `running=true`；旧 session 无阶段信息；缺少 `isn` 时只有停止失败，没有安全的本地解除入口；启动指令发送后响应丢失与扫描超时需要采用不同处理。
- 待处理：引入分阶段会话，发送前明确失败自动回滚，发送后不确定保留；为历史异常会话提供带说明的本地解除操作。

## 2026-09-10 首页少量进行中任务被拉伸
- 现象：首页“进行中”只有一个或两个任务时，人物/任务区域没有稳定靠左排列。
- 根因：每项宽度使用 `constraints.maxWidth / tasks.length`，一个任务占整行、两个任务各占半行，布局会随数量拉伸。
- 待处理：使用固定三列宽度并左对齐排列，一个任务占左一列、两个任务占左两列。

## 2026-09-11 当前工作区 APK 构建失败
- 现象：`flutter analyze` 报 4 个 Dart 错误；`flutter build apk --release` 在 `:app:compileFlutterBuildRelease` 的 kernel snapshot 阶段失败。
- 根因：未完成的热水分阶段改动存在可空 `session` 传参、缺失 `_discardSession` 方法，以及未导入 `HotwaterSessionPhase` 三处代码问题。
- 旁支：`bash tools/release/build_android_release.sh --validate-only` 读取根目录 `public/version.json`，与 `pubspec.yaml` 的 2.1.1 不一致；项目实际版本清单为 `assets/public/version.json`。当前也没有 `android/key.properties`，正式签名配置尚未提供。
- 处理：已纳入 OpenSpec change `fix-hotwater-session-recovery-and-android-build`，Dart 编译阻塞、状态迁移、旧 session 清理、回归测试和发布脚本路径均已修复；正式签名配置仍由发布环境提供。

## 2026-09-11 默认 APK 构建缺少 arm 引擎快照
- 现象：`flutter build apk --release` 已进入 Gradle，但在 `:app:compileFlutterBuildRelease` 的 AOT 阶段失败。
- 原因：当前 Flutter 缓存没有 `android-arm-release/darwin-x64/gen_snapshot`，默认多架构构建仍会尝试生成 32 位 ARM 产物；`android-arm64-release` 与 `android-x64-release` 缓存完整。
- 处理：优先使用 `--target-platform android-arm64` 生成实际发布验证所需的单架构 APK；该环境问题不改变 Dart 源码编译结果。
- 验证：`flutter build apk --release --target-platform android-arm64` 成功；产物为本地 debug 签名验证包，正式发布仍需补充 `android/key.properties` 和 keystore。

## 2026-09-14 Flutter 3.44 正式发布目标不包含 32 位 ARM
- 现象：正式发布脚本默认的 split APK/AAB 构建会尝试 `android-arm`，但 Flutter 3.44.4 本机没有 `android-arm-release/darwin-x64/gen_snapshot`，`flutter precache --android` 也不会提供该文件。
- 影响：源码分析、全量测试和签名配置均通过，但默认发布脚本无法生成正式 APK/AAB。
- 处理：发布脚本显式使用当前 SDK 可用的 `android-arm64,android-x64`，同时生成 split APK、通用 `app-release.apk` 和 AAB；32 位 ARM 设备不在本次正式包支持范围内。正式脚本已验证通过。

## 2026-09-14 正式 AAB native library 符号剥离失败
- 现象：arm64/x64 split APK 和通用 APK 构建成功，但 `flutter build appbundle --release` 在 `Release app bundle failed to strip debug symbols from native libraries` 处失败。
- 影响：正式 AAB 尚未生成，当前只能核验 APK，不能宣称完整 Android 发布完成。
- 处理：安装 Android Command-line Tools，并将其置于当前 Android SDK 的 `cmdline-tools/latest`；AAB 中已确认存在 arm64/x64 的 `libapp.so.sym` 与 `libflutter.so.sym`，完整正式发布脚本已通过。
## 2026-09-14 GitHub release target 使用短 SHA 被拒绝
- 现象：创建 `v2.1.2` 时传入短提交号 `b579008`，GitHub API 返回 `Release.target_commitish is invalid`。
- 影响：第一次请求未创建 release，也未上传资产。
- 处理：改用已推送的 `main` 分支作为 release target 重试。
# 2026-09-20 真实联调待验证项

- 自动化129项通过不覆盖真实BLE水流或支付宝包名/签名兼容；两者不能标为现场通过。
- 当前尚缺手机系统、现场洗浴服务/设备、日志和支付业务/金额范围。先等待第一阶段条件确认，不自动执行支付或设备控制。
# CI-02 / 2026-09-20：远端首跑 SDK 工具路径失败

- 提交1bd382da2b67b522ace34417f067eb6db912f887已推送main；Flutter Android CI运行35479232027，分析与全量测试通过。
- Prepare Android SDK明确报sdkmanager: command not found，退出127；Debug构建与artifact上传未执行。预装SDK工具不等于可直接从PATH调用，本地构建成功不能覆盖此Linux路径问题。
- 待处理：显式解析runner Android SDK中的sdkmanager路径并检查可执行性，或配置专用Android setup步骤；当前仅记录，没有追加修改/推送。checkout/setup-java另有Node20及setup-java v4弃用告警，不是本次失败原因。
- 用户已授权修复并再推送：按官方runner安装脚本确认的cmdline-tools/latest/bin布局，优先ANDROID_HOME、回退ANDROID_SDK_ROOT；检查空路径/不可执行，显式传sdk_root。无需改变构建/业务逻辑；远端结果待本次运行核实。
- actionlint、bash语法与4种隔离路径场景通过。临时Ruby验证命令首次因谓词比较缺空格出现语法错误，修正后通过；不是workflow脚本错误。
