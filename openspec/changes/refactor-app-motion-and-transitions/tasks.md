## 1. 实施基线与动效基础

- [x] 1.1 重新阅读用户协作规则及本变更文档，记录当前工作区基线；清点各页点击、路由、弹层、列表和异步状态入口，形成覆盖清单。
- [x] 1.2 运行现有 analyze/test 建立基线，将实际发现的问题追加到 bug.md，区分既有问题与本变更新问题。
- [x] 1.3 在 ShuiMotion 建立 B 风格语义令牌及系统减少动效策略，统一开场与首次内容交接。
- [x] 1.4 改造 ShuiPressable 的轻压、取消、禁用、焦点和键盘激活；覆盖公共按钮、卡片、图标入口，避免原生控件双重反馈。
- [x] 1.5 验证一次激活一次回调、拖动取消、禁用切换和减少动效；提供首轮手感预览，更新任务并交用户审查后继续。

## 2. 导航、底栏与浏览现场

- [x] 2.1 建立导航行为测试：账号多级返回、来源返回、重复目的地、快速切换及任务退出清理。
- [x] 2.2 保留 ShuiRoute 模型，引入 Navigator/Page 详情栈与转场元数据，统一页内/系统返回及支持的返回手势。
- [x] 2.3 完成主 Tab 短距离顺向切换、选中标记移动和详情正反向转场，旧页禁用交互与辅助功能焦点。
- [x] 2.4 保留 Tab 滚动位置、订单分类和有效选择；接入离屏/恢复生命周期，验证页面计时器和轮询不重复。
- [ ] 2.5 按设计表落实底栏显隐，统一内容占位和安全区，替换相关页面硬编码底栏 padding，处理键盘 inset。
- [x] 2.6 验证快速切换、转场中返回、保活恢复、窄屏和键盘布局；更新任务并交用户审查后继续。

## 3. 弹窗与菜单

- [x] 3.1 建立复用弹层宿主：遮罩、进入/退出、焦点恢复、点击拦截和最上层返回处理。
- [x] 3.2 接入添加设备、预设选择、设备操作菜单及改名，完成锚点菜单和内部步骤共享遮罩过渡。
- [x] 3.3 接入更多选项的信息、版本、关于及应用权限引导；保留系统权限弹窗的原生行为。
- [x] 3.4 验证快速开关不穿透、内部返回不误关父层、200% 字体与键盘下操作可达；更新任务并交用户审查后继续。

## 4. 列表、分类与局部状态

- [x] 4.1 为设备、订单、进行中任务提供稳定业务身份，实现条目级增删、保留项让位及空态交接。
- [x] 4.2 完成订单分类、洗衣选项、账号设备选择与默认系统选中反馈，避免重播无关页面内容。
- [x] 4.3 改造 RuntimeStatusBanner、热水状态及其他动态提示，覆盖无消息到有消息及消失时的高度过渡。
- [ ] 4.4 添加/改名成功关闭弹层并突出对应设备，失败保留输入；确保删除只移除对应项。
- [ ] 4.5 验证首条/末条增删、重复轮询、任务组合变化和长错误文本；更新任务并交用户审查后继续。

## 5. 登录与业务操作反馈

- [x] 5.1 统一登录、验证码、刷新、开关水、创建/取消/启动洗衣及支付按钮的处理中和结果交接，保持外部尺寸稳定与请求防重。
- [x] 5.2 完成住理、798、U净登录表单到账号信息的原地过渡，处理键盘焦点及失败重试，保持既有默认系统规则。
- [x] 5.3 衔接扫码进入、取消、识别以及外部支付返回，验证重复识别不重复导航，支付恢复不提前显示成功。
- [ ] 5.4 验证慢请求、失败、重复点击、前后台往返和账号状态刷新；更新任务并交用户审查后继续。

## 6. 接水完成结果

- [x] 6.1 在既有终态确认点建立按订单 ID 绑定的结果快照，先捕获必要摘要再清理活动订单，不改变终态判定与轮询规则。
- [x] 6.2 移除 Shell 自动回首页逻辑；接水页原地呈现成功/取消等真实结果，提供主动返回首页，保持底栏隐藏。
- [x] 6.3 定义新流程及离开时的展示状态清理，阻止旧订单晚到响应覆盖新结果；保留业务历史。
- [x] 6.4 测试活动订单清空后结果仍在、失败不误报、取消、重复终态、旧响应、新订单及主动返回；更新任务并交用户审查后继续。

## 7. 整体验收

- [x] 7.1 对照交互覆盖清单逐项验收，消除遗漏的直接切换及重复强调，确保同一动作只有一个主要视觉焦点。
- [x] 7.2 验证减少动效、键盘操作、辅助功能焦点、窄屏/宽屏、200% 字体、安全区和横竖屏，无不可读重叠或操作遮挡。
- [x] 7.3 运行 flutter analyze、完整 flutter test 和 git diff --check，处理本变更回归，不重建已删除的 golden 测试体系。
- [ ] 7.4 在可用 Android/iOS 真机 profile 模式执行预热后的导航、弹层、列表和异步反馈脚本，记录设备/刷新率、UI/raster 帧时间及异常；缺少设备的项目保持未完成。
- [ ] 7.5 提供关键流程录屏或可运行预览，按用户反馈微调 B 风格幅度和节奏，记录最终验证结果及未解决问题，交用户最终审查。
